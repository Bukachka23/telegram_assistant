llm:
  default_model: "minimax/minimax-m2.7"
  max_tokens: 4096
  temperature: 0.7

vault:
  path: "/Users/bukachiyboss/Documents/OBS"
  default_folder: "notes"

conversation:
  session_timeout_minutes: 30
  max_history_messages: 50

streaming:
  draft_interval_ms: 800

channels:
  monitor: []

scheduler:
  tz_offset_hours: 2  # Vienna CEST (UTC+2); change to 1 in winter (CET)

telegraph:
  enabled: true
  threshold_chars: 8000
  author_name: "Telegram Assistant"
  author_url: ""
